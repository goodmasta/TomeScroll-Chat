# Working conventions for this repo

- Commit finished work/builds to the local git repo as normal, but do **not** `git push` to GitHub (`origin`) unless the user explicitly asks for it in that turn. Local commits accumulate freely; publishing to GitHub is a separate, user-gated step.
